import type {
  DomAdapter,
  MetricsOptions,
  MetricsSummary,
  NormalizedMetricsOptions,
  RenderMetric,
  TestingPluginOptions,
} from '@pokujs/dom';

export type AngularDomAdapter = DomAdapter;
export type AngularMetricsSummary = MetricsSummary;
export type AngularMetricsOptions = MetricsOptions;
export type AngularTestingPluginOptions = TestingPluginOptions;

export type {
  NormalizedMetricsOptions,
  RenderMetric,
};
